# 1.5.1 DeepSeek Flash FULL Embedded

- DeepSeek model fixed to `deepseek-v4-flash`.
- API key embedded in `.env.local` for this private build.
- Sample values -> DeepSeek ON.
- Retrieved document text -> DeepSeek ON.
- Structured evidence -> DeepSeek ON.
- Schema reasoning / JOIN reasoning / query rewrite -> ON.
- Pre-LLM redaction -> OFF.
- Existing specialist engines remain active.
