-r requirements.txt
# Vision inference / tracking
onnxruntime>=1.20
supervision>=0.27
# Streaming + batch anomaly detection
river>=0.23
pyod>=2.0
# Scheduling optimization
ortools>=9.12
# OCR (large optional dependency; install only when needed)
paddleocr>=3.0
# Edge acceleration (large optional dependency; install only on target Intel deployment)
openvino>=2025.0
# Causal inference (requires an explicit DAG / confounder design)
dowhy>=0.13
evidently>=0.7
duckdb>=1.3
sktime>=0.38
